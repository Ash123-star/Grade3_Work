# 企业协作智能面板 PRD

## 1. 文档信息

- 版本：V1.0（规划基线）
- 日期：2026-09-21
- 产品负责人：
- 技术负责人：
- 评审人：

## 2. 产品定义

企业协作智能面板为企业内部提供统一的任务、日志、组织权限、审批、消息、报表和 AI 工作导航。交付 Android App 与 Web 工作台，共用 Spring Boot 服务端、PostgreSQL/Redis 数据和统一权限规则，首期只启用单公司。

### 目标

1. 让管理者在授权范围内派发任务、查看下属日志并完成复核。
2. 让员工以每天一份主日报记录工作、阻碍和计划，并关联任务。
3. 用可追溯、有来源的 AI 建议帮助用户整理工作和规划下一步。
4. 用事件、版本和审计记录保证任务与业务修改可追踪。

### 不做事项

首期不做对外社交、不自动发送外部消息、不让模型直接写数据库、不以日报字数自动评价员工。

## 3. 用户与权限

角色为管理员、创始人、部门老总、团队长、员工。采用 RBAC + 数据范围（本人/团队/部门/全公司）。服务端对每次查询、搜索、下载、导出和 AI 检索鉴权。注册只能创建员工，部门确认前不能读取内部数据；管理员角色只能由管理员授予。停用账号撤销会话，调岗重新计算范围。

## 4. 核心流程

### 4.1 任务流程

派发 → 接收 → 反馈 → 验收 → 归档。转派、撤回、期限变更均产生事件、通知和审计。一个任务只有一名主负责人，多人独立负责时拆分子任务。派发页不包含“完成情况”列或状态输入框，详情页用时间线呈现流转。

### 4.2 日志流程

自动保存草稿 → 提交 → 上级查看/评语 → 必要时提交修订并按配置审核。每人每天一份主日报，含今日工作、关联任务、阻碍、明日计划和可选工时。日/周/月视图与日志使用同一业务日期区间。

### 4.3 修改审核

公司展示、组织、角色权限、已发布关键信息和已提交日报的修改生成候选版本。审核按团队长、部门老总、指定公司复核人路由，申请人不能自审；无独立复核人保持待审。旧版本在新版本通过前继续生效，驳回必须填写原因。

## 5. 端侧需求

### Android

底部从左至右为“我的｜AI 地图｜日志｜视图｜导图”，默认进入导图。导图为四象限：公司十大重要展示项、公司十大派发任务、个人十大重要展示项、个人日志。管理者有派发资格时显示右下角加号；无资格时隐藏且 API 拒绝。

### Web

使用左侧导航、中间工作台、右侧详情抽屉。首页按角色显示四象限、组织树、审批和经营总览。任务支持宽屏表格/卡片切换，仍不添加完成情况字段。卡通素材使用 OpenMoji，保留 CC BY-SA 4.0 署名与来源。

## 6. AI 地图

围绕目标连接任务、重点、日报、阻碍和风险。移动端默认展示今天优先处理的三件事，Web 支持缩放、拖拽、过滤和详情。AI 输出必须带来源、生成时间和可编辑内容，始终是建议草稿，用户确认后才创建任务或发布。服务端先筛选权限范围，再调用 DeepSeek；密钥不进入客户端、仓库和普通日志。AI 故障不得影响日志与任务基础功能。

## 7. 数据与接口

核心实体包括公司、部门、团队、用户、角色、任务、任务事件、日报、日报版本、审核请求、通知、审计、AI 作业和 AI 地图节点/边。所有业务表带 company_id；日报以用户+业务日期唯一，重要修改用版本号防并发覆盖。API 域：`/auth`、`/organization`、`/dashboard`、`/tasks`、`/logs`、`/reviews`、`/notifications`、`/analytics`、`/ai`、`/ai-maps`。

## 8. 非功能与验收

- HTTPS、密码安全存储、统一错误码与追踪 ID、审计留痕。
- 任务派发使用幂等键；通知使用事务 outbox；数据库迁移版本化。
- 移动端点击目标至少 48dp，支持字体放大和无障碍对比。
- 验收覆盖注册角色注入、对象 ID 越权、导航顺序、无权限派发、日志范围、审核自审、AI 降级和审计追溯。

## 9. 里程碑

- M1：工程、迁移、登录注册、组织权限、五栏、四象限、日志、任务闭环。
- M2：分层审核、日周月联动、上级穿透、消息、Web 工作台、统计导出。
- M3：DeepSeek 总结、AI 地图、建议确认、离线草稿冲突处理、双端验收和部署。

## 10. 待补信息

人员信息、正式架构/流程/ER/原型图、OpenAPI、迁移脚本、测试报告、部署手册和 OpenMoji 资产署名记录在评审后补齐。
