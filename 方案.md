# 企业协作智能面板方案

## 1. 交付与技术选型

面向企业内部协作，交付 Flutter Android 软件及独立 Web 工作台，共用一套服务端和权限规则。本次完成方案与 OpenSpec 配置，后续按首个变更逐项实现，不把规划视为已完成的软件。

建议技术栈：
- Android：Flutter + Material 3 + Riverpod + go_router + Dio；Drift 保存离线日志草稿。
- Web：Vue 3 + TypeScript + Vite + Pinia，ECharts 报表、Vue Flow 关系地图。
- 服务端：Spring Boot 3 + PostgreSQL + Redis，REST API + SSE 站内消息刷新。
- AI：服务端调用 DeepSeek API，客户端不持有密钥。
- 部署：Docker Compose + HTTPS；数据库迁移版本化；UTC 存储时间、默认 Asia/Shanghai 展示。

建议目录：mobile/、web/、server/、deploy/、docs/。最终交付 APK、Web 构建产物、API 文档、迁移和种子脚本、环境变量示例、测试及部署说明。

## 2. 注册、组织与权限

预置产品部、市场部、技术部，管理员可改名或增设。注册填写账号、密码、手机号并选择部门；手机号唯一，所有新账号只能创建为员工，注册请求中传入管理角色必须被服务端拒绝。部门选择是申请，管理员确认归属前不能读取公司内部信息。短信服务未接入时不得假装验证码校验成功。

初始化脚本通过环境变量创建首个管理员，无公开默认密码，首次登录强制改密；脚本幂等，不覆盖现有账号。角色仅由管理员授予。组织为公司→部门→团队→员工，另有直属上级关系，禁止循环汇报关系。

| 角色 | 可查看范围 | 操作能力 |
|---|---|---|
| 管理员 | 全公司业务数据和系统信息 | 全量修改、组织/权限配置、审核策略、审计与账号停用 |
| 创始人 | 全公司经营信息和所有人员工作面板 | 汇总收集、跨部门分发任务、查看报表 |
| 部门老总 | 本部门及下属团队 | 向下穿透、派发任务、复核及查看下属日志 |
| 团队长 | 本团队及下属人员 | 向下穿透、派发任务、复核及查看下属日志 |
| 员工 | 自己的信息及明确授权的公司内容/任务 | 个人面板、日志、任务反馈、AI 辅助 |

采用 RBAC + 数据范围（本人/团队/部门/全公司），每次服务端查询、下载、导出、搜索和 AI 检索都鉴权，不能只隐藏按钮。管理者默认拥有范围内任务派发资格，可通过权限开关收回。调岗重新计算范围，停用账号撤销会话。

“全量穿透”指授权业务信息，不包含密码、私人备注、未提交草稿。穿透面板清晰显示“正在查看某某”，可一键返回本人面板，不以他人身份提交内容。

## 3. 分层审核和修改

管理员拥有全量修改能力，但正式业务变更仍通过审核留痕：
1. 普通私人草稿和私人备注即时保存。
2. 公司展示、组织、角色权限、已发布关键信息及已提交日报的修订生成候选版本，旧版本在审核通过前继续生效。
3. 按团队长→部门老总→指定公司复核人路由，跳过无该岗位层级；申请人不能审核自己的修改。
4. 无独立复核人时保持待审，不能自动通过。部署初始化是唯一引导例外，创建首个管理员并记录初始化事件；正式业务启用前补齐至少一名独立复核人。
5. 驳回需填写原因，可修订重提；通过时校验版本，避免覆盖他人的后续变更。审计记录包含操作者、时间、修改前后值、理由和审核链。

普通任务反馈属于新增业务事件，无需每次审批；修改已发布任务的负责人、期限或关键内容走配置的复核策略。审核通知进入消息中心，管理者在 Web 审批工作台或 App“我的→审批”处理。

## 4. Android 页面

### 底部导航

严格按需求从右往左：导图、视图、日志、AI 地图、我的。
因此屏幕从左往右：我的｜AI 地图｜日志｜视图｜导图。
默认进入最右侧的导图，位置不影响默认路由。

### 导图首页

保留四象限语义，上下两行，每行两块；小屏先展示标题和前 2～3 项，再点开全部，不在一屏挤下 40 条。

| 位置 | 内容 | 交互 |
|---|---|---|
| 左上 | 公司十大重要展示项 | 只读，可编辑个人备注 |
| 右上 | 公司十大派发任务 | 只读，可编辑个人备注 |
| 左下 | 个人十大重要展示项 | 本人可新增、编辑、排序、归档 |
| 右下 | 个人日志 | 打开日志编辑器，草稿可改，已提交修订留痕 |

公司卡片由有权限人员在独立发布入口维护，首页上方对所有角色保持只读；任务展示遵守受众权限，不能把私密任务公开给全员。十大指最多置顶 10 项，超出需替换或归档，空项不造假。私人备注仅本人可见。

有任务派发资格的人右下角显示圆形“＋”，位于底栏上方，避免遮挡卡片；点开任务表单，支持选择接收人。无资格时隐藏，直接请求派发 API 也返回拒绝。

### 个人面板

采用类似人人网的个人主页：头像、姓名、部门、介绍、工作动态、个人重点、日志时间线。上级可按组织树穿透下属面板，默认只读；个人头像和介绍可修改，组织归属和角色不可自行修改。

### 视图与日志联动

视图提供日/周/月切换，展示同一时间区间的日志和任务，点击跳回对应详情。周一作为每周开始；按本地业务日期划定区间，再转换 UTC 查询。两页共享日期筛选，跨月跨年保持一致。

日志页包含“我的日志”，管理者额外显示“下属日志”，人员选择器仅出现管理范围内人员。每人每天一份主日报，内容含今日工作、关联任务、阻碍、明日计划、可选工时；支持自动保存草稿、提交、历史修订。上级查看已提交内容，已读记录和评语单独保存。周报月报聚合日报，AI 总结标注来源，允许人工编辑。

## 5. 任务派发

参考截图的任务分组、名称、截止日期、紧急程度、进度备注、负责人，但移动端使用单列卡片及纵向表单，避免横向大表格。

派发表单：任务名称、所属分组、内容、截止日期/时间、紧急程度（紧急/普通/不紧急）、进度备注、主负责人、可选协作人、附件。名称、截止时间和负责人必填。人员选择器支持姓名/部门搜索，只返回可派发范围内在职人员；一个任务一名主负责人，需要多人独立负责时拆为子任务。

**派发页面不出现“完成情况”列、输入框、筛选或替代状态标签。** 为支持流转，后台保留派发→接收→反馈→验收→归档事件，详情页用操作时间线呈现；员工提交成果反馈，派发者验收，不在派发表格增加完成情况。转派、撤回、期限变更均留痕并通知，重复提交通过幂等键防止重复派发。

## 6. 通知、分析与上级查看

站内消息包含派发提醒、截止提醒、审批待办/结果、日志待阅；支持未读数、全部已读、按类型筛选和跳转。使用事务 outbox 避免业务成功但通知丢失，定时提醒去重。首期站内消息，后续 Android 厂商离线推送单独接入。

分析指标：派发数量、逾期数量、按期验收率、日报提交率、日志待阅数、部门工作分布。每份报表显示统计区间、口径、分母和更新时间；任务统计以实际流转事件计算，不能因派发页没有完成情况而失去分析能力。不按日报字数自动给员工打分。

上级查看日志的入口固定在“日志→下属日志”；任务和报表可由管理者面板进入。员工不能切换他人 ID 获取数据。导出和聚合结果与详情保持相同范围。

## 7. AI 地图与 DeepSeek

AI 地图是一张可操作的工作导航图，围绕个人或管理范围内目标连接任务、重点、日报、阻碍和风险。手机默认“今天优先处理的三件事”卡片，支持切换关系图；Web 支持缩放、拖拽、过滤和右侧详情。

功能：
- 从日报提取目标、成果、阻碍并生成候选节点。
- 将日志和任务关联，展示任务依赖及临期提醒。
- 总结当天/本周工作，给出带来源的文本摘要。
- 推荐下一步行动与候选负责人，候选人员必须来自当前派发范围。
- 选择节点后让 AI 解释风险、拆分步骤或起草任务。

所有 AI 输出为建议草稿，用户确认后才创建任务或发布内容。保留来源链接、生成时间、可编辑内容；没有来源的数据不作为确定事实呈现。非 AI 规则负责实际逾期判断，避免模型猜测。

服务端先按用户范围筛选数据，再向 DeepSeek 发送必要业务文本；不发送手机号、密码、私人备注。模型无直接数据库写权限，输出经过结构校验。日志中的提示语被视为数据，不得越过权限或触发写操作。

配置 DEEPSEEK_API_KEY、DEEPSEEK_BASE_URL、DEEPSEEK_MODEL，模型名称可配置，部署时按官方文档选择。实现流式响应、异步 AiJob、超时重试、取消、限流、每日预算和用量统计。失败保留输入、支持重试，基础日志和任务功能不依赖 AI 可用性。密钥不进入客户端、仓库和普通日志。

## 8. Web 卡通风格与素材

Web 独立设计，不照搬手机底栏。使用左侧导航、中间工作台、右侧详情抽屉；首页展示四象限大卡片，管理者多出组织树、审批和经营总览。任务列表支持宽屏表格/卡片切换，同样不添加完成情况字段。

设计语言：奶油白底色 #FFF9F0、薄荷绿 #75C9B7、珊瑚橙 #FF9D87、深色正文 #263238；圆角卡片、彩色描边、小幅阴影。卡通插图用在引导、空状态和 AI 助手，正文保持清晰与足够对比。点击目标至少 48dp，支持字体放大，颜色同时配文字或图标。

已选定素材库：[OpenMoji](https://github.com/hfg-gmuend/openmoji)，官网 https://openmoji.org/ 。彩色手绘风 SVG 可用于任务、日历、地图、团队等图标。
已直接核对其 [LICENSE.txt](https://github.com/hfg-gmuend/openmoji/blob/master/LICENSE.txt)，许可为 **CC BY-SA 4.0**，不是 Apache-2.0。实施时保留作者、来源、许可链接；修改素材标明改动并遵守相同方式共享要求。资产落地 mobile/assets 与 web/public/assets，记录版本和署名，不使用外部热链。本次选型未下载完整素材包。

素材检索直接使用官网或 GitHub，不使用百炼。

## 9. 数据模型与 API 边界

核心表：companies、departments、teams、users、roles、permissions、user_roles、reporting_relations、dashboard_items、private_notes、tasks、task_assignees、task_events、daily_logs、log_versions、log_reads、review_requests、review_steps、audit_records、notifications、outbox_events、ai_jobs、ai_map_nodes、ai_map_edges。

所有业务数据包含 company_id，首期只启用单公司；公司隔离先于角色范围。日报以用户+业务日期唯一，重要变更用版本号做并发校验。个人备注不进入公共详情响应。

API 域：/auth、/organization、/dashboard、/tasks、/logs、/reviews、/notifications、/analytics、/ai、/ai-maps。返回统一错误码和追踪 ID；权限、分页、区间过滤在服务端完成，统一生成 OpenAPI 契约供两端使用。

## 10. 里程碑和验收

M1：工程与迁移、管理员初始化、注册登录、组织和权限、Android 五栏、四象限、日志草稿/提交、任务派发基础闭环。
M2：分层复核、日周月联动、上级穿透、站内消息、Web 卡通工作台和统计导出。
M3：DeepSeek 日报总结、AI 地图与建议确认、离线草稿冲突处理、双端验收和部署。

核心验收：
1. 注册强制员工，不能通过传入角色或对象 ID 越权。
2. 导航从右到左符合需求，默认打开导图；上方只读且个人备注独立。
3. 无派发资格用户不显示加号，后端也拒绝；派发页无完成情况。
4. 日志页能查看权限内下属，日周月与视图使用一致区间。
5. 提交修改需要复核时旧版继续有效，自审被拒绝，审计可追溯。
6. AI 不可用不影响基础业务，建议不会自动派发任务。
7. Flutter 分析/测试和 APK 构建、Web 类型检查/测试和生产构建、后端权限及审批集成测试通过后才能交付；当前尚未运行这些应用测试。

## 11. 第二阶段

创始人可主动发布对外业务简介、能力标签和合作诉求，通过行业、地区和供需标签匹配其他公司。仅索引主动公开的信息，不默认公开内部日志和人员。匹配结果只作为候选，用户决定是否发起联系，不能由 AI 自动发送。
